version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: remind_repeat_bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # You can override START_CMD here or in .env
      - START_CMD=${START_CMD}
    volumes:
      - ./:/app:ro        # change to :rw if you want hot-reload
    # ports:               # uncomment if the app exposes a port
    #   - "8000:8000"
    # depends_on:
    #   - redis
    #   - db

# Example helper services (uncomment and adjust if needed)
#  redis:
#    image: redis:7-alpine
#    restart: unless-stopped
#    volumes:
#      - redis-data:/data
#
#  db:
#    image: postgres:15-alpine
#    restart: unless-stopped
#    environment:
#      POSTGRES_USER: postgres
#      POSTGRES_PASSWORD: example
#      POSTGRES_DB: remind_db
#    volumes:
#      - pgdata:/var/lib/postgresql/data

#volumes:
#  redis-data:
#  pgdata:
